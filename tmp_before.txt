"use client"

interface BlogContentProps {
  intro: string
  sections: Array<{
    title: string
    description: string
    items: string[]
    paragraph?: string
  }>
}

export function BlogContent({ intro, sections }: BlogContentProps) {
  return (
    <article className="prose prose-invert max-w-none space-y-8">
      {/* Introduction */}
      <section id="intro">
        <p className="text-lg leading-relaxed text-foreground" style={{ whiteSpace: "pre-line" }}>{intro}</p>
      </section>

      {/* Sections */}
      {sections.map((section, index) => (
        <section key={index} id={`section-${index}`} className="space-y-4" style={{ whiteSpace: "pre-line" }}>
          {/* Subtitle */}
          <h2 className="text-2xl font-bold text-foreground md:text-3xl" style={{ whiteSpace: "pre-line" }}>{section.title}</h2>

          {/* Description */}
          <p className="text-lg leading-relaxed text-muted-foreground" style={{ whiteSpace: "pre-line" }}>{section.description}</p>

          {/* List Items with Pointers */}
          <ul className="space-y-3 pl-6">
            {section.items.map((item, itemIndex) => (
              <li key={itemIndex} className="flex gap-3 text-muted-foreground" style={{ whiteSpace: "pre-line" }}>
                <span className="mt-1.5 inline-flex h-2 w-2 rounded-full bg-primary flex-shrink-0" style={{ whiteSpace: "pre-line" }} />
                <span className="leading-relaxed">{item}</span>
              </li>
            ))}
          </ul>

          {section.paragraph && (
            <p className="text-base leading-relaxed text-muted-foreground pt-2" style={{ whiteSpace: "pre-line" }}>{section.paragraph}</p>
          )}
        </section>
      ))}

      {/* Conclusion */}
      <section id="conclusion" className="space-y-4 border-t border-border pt-8" style={{ whiteSpace: "pre-line" }}>
        <h2 className="text-2xl font-bold text-foreground md:text-3xl">Conclusion</h2>
        <p className="text-lg leading-relaxed text-muted-foreground">
          The integration of AI into web development is not a distant future—it's happening now. As developers, our
          responsibility is to embrace these tools while maintaining the craftsmanship and ethical standards that define
          our profession. The most successful teams will be those who view AI not as a replacement for human creativity,
          but as a powerful amplifier of it. The future of web development belongs to those who can harness AI's
          capabilities while preserving the human touch that makes great software.
        </p>
      </section>
    </article>
  )
}
